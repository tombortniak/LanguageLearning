import 'package:flutter/material.dart';

class LearningResultsPage extends StatelessWidget {
  const LearningResultsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Wyniki'),
      ),
    );
  }
}
